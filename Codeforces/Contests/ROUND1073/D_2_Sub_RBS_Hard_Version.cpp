#include <bits/stdc++.h>
using namespace std;

#define int long long
#define ld long double
#define debug(x) cout << "(" << #x << " : " << (x) << ")\n"
#define debughere cout << "HERE\n"

const int MOD = 998244353;

void solve(){

    /*
        for all subsequences of s, solve problem D1
        and sum them up (-1 becomes 0)

        what if i fix the length? then id solve the problem for all lengths
        for each length:
            for each D1 answer value:
                count how many valid subsequences for the fixed length
                such thaat 
            count the number of subsequences of that length
            that are valid, 
    */
}

signed main(){
    ios_base::sync_with_stdio(false), cin.tie(NULL);
    int t = 1;
    // cin >> t;
    while (t--)
        solve();
    return 0;
}